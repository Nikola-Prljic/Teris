#pragma once

#include <string>
#include <raylib.h>
#include <raymath.h>

class Interface
{
    private:


    public:

        Interface();
        ~Interface();
        void draw(const Model &model, Vector3 pos);


};
